// 设置用户信息
export const SET_USER_INFO = (state, data) => {
  state.userInfo = data;
};

// 设置技术引擎产品列表
export const SET_PRODUCT_LIST = (state, data) => {
  state.productList = data;
};

// 设置用户的应用列表
export const SET_APP_LIST = (state, data) => {
  state.appList = data;
};


